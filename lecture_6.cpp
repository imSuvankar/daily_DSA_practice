#include <iostream>
#include <math.h>
using namespace std;


int main() {
    

    /*
    // DECIMAL TO BINARY
    
    cout << "enter your decimal integer: ";
    int n; cin >> n;

    int ans = 0, i = 0;
    while(n) {
        int last_bit = n & 1;
        n /= 2;
        ans += last_bit*pow(10,i);
        i++;
    }

    cout << ans;
    */



    /*
    // BINARY TO DECIMAL
    cout << "enter your binary integer: ";
    int n; cin >> n;

    int ans = 0, i = 0;
    while(n) {
        int last_digit = n % 10;
        n /= 10;
        if (last_digit == 1) ans += pow(2, i);
        i++;
    }

    cout << ans;
    */



    
}